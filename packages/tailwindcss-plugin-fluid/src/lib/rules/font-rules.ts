import type { RuleDefinitions } from '../utils/types.js'

export const fontRules: RuleDefinitions = {
  'font-size': { key: 'fontSize', properties: ['font-size'] },
  'indent': { key: 'textIndent', properties: ['text-indent'] },
  'leading': { key: 'lineHeight', properties: ['line-height'] },
}
